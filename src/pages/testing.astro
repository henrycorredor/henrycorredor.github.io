---
// import type { ImageMetadata } from "astro";
// import { Image } from "astro:assets";
import { getCollection } from "astro:content";

// type ImageImport = Record<string, () => Promise<{
//     default: ImageMetadata;
// }>>

const allBlogPosts = await getCollection("works");
// const galleryPictures: Record<string, ImageImport> = {}
// allBlogPosts.forEach(_ => {
//   galleryPictures[_.id] = import.meta.glob<{ default: ImageMetadata }>(`/src/content/testing/otro/*.{jpeg,jpg,png,gif}`);
// })
// console.log(galleryPictures);

console.log(allBlogPosts)
---
<div>Pagina de testeo de los proyectos</div>